# 2026-03-06

- Session en cours: debug browser-only MiniMax AI Motion Coach pour FormCheck.
- Hypothèse principale confirmée en reproduction manuelle: modal login Google tardif au moment du `send` sur MiniMax.
- Travail engagé: correction du flow Playwright navigateur, ajout de tests ciblés, validation locale, puis push/deploy.
- Root cause confirmé sur le flow MiniMax browser-only: modal login Google tardif au moment du `send`, non géré correctement, plus détection de retour auth trop permissive sur la page MiniMax déjà ouverte.
- Correctif implémenté dans `src/analysis/minimax_motion_coach.py`:
  - détection explicite du modal login
  - gestion écran de consentement Google
  - attente d'une vraie page MiniMax authentifiée
  - retry d'upload/envoi après auth avec réouverture AI Motion Coach si nécessaire
- Tests ajoutés dans `src/tests/test_minimax_motion_coach.py` pour couvrir ces cas.
- Validation locale: `python3 -m unittest discover -s tests -v` => 61/61 OK.
- Commit/push: `9c02faa` `fix minimax browser auth retry flow`.
- Audit live Render/WhatsApp lancé après nouvel envoi vidéo user.
- Audit live Render après envoi vidéo user: logs prod montrent un échec à `2026-03-06 09:08:27` avec `MiniMax browser flow failed: search box not found`.
- Cause réelle: l'envoi WhatsApp et le download vidéo passent, mais le flow AI Motion Coach essayait d'ouvrir le chat avant d'avoir garanti l'auth browser, puis tombait sur un fallback `/experts` fragile.
- Correctif poussé: commit `8ae43fd` `fix minimax expert chat bootstrap flow`.
- Changements: auth navigateur avant ouverture du chat expert + support CTA direct `Type to chat with AI Motion Coach` + fallback experts plus robuste.
- Validation locale après correctif: `63/63` tests OK.
- Nouveau retour user: encore erreur après fix bootstrap MiniMax; audit live relancé.
- Nouveau check logs live après retour user `encore erreur`:
  - log prod `2026-03-06 09:42:57,364 [ERROR] formcheck.pipeline: MiniMax Motion Coach échoué: MiniMax browser flow failed: search box not found`
  - stack sur instance live précédente `8ae43fd` (lignes `2600` et `2289`).
- Cause affinée: en headless Render, le CTA `Type to chat with AI Motion Coach` est cliquable via accessibilité Playwright (`get_by_role`) mais pas de manière fiable via nos sélecteurs CSS.
- Repro manuelle validée: `getByRole('button', { name: /Type to chat with AI Motion Coach/i })` clique bien, et ensuite `.tiptap-editor`, `input[type=file]`, `#input-send-icon` sont présents.
- Correctif poussé: `8c41fae` `fix minimax cta bootstrap in headless`.
- Validation locale: `64/64` tests OK.
- Déploiement Render démarré pour `8c41fae` (`dep-d6la5jvpm1nc73btldag`), pas encore live au dernier check.
- Nouveau retour user `erreur` apres re-envoi video; audit live Render confirme que meme la build `8c41fae` echoue encore a `2026-03-06 09:57:34` avec `MiniMax browser flow failed: search box not found` dans `_open_motion_coach_chat`.
- Cause retenue: le fallback `/experts` dependait encore trop d'un DOM fragile (`Search experts`) et ne laissait pas assez de temps a l'hydratation UI MiniMax avant de conclure a un echec.
- Correctif local applique dans `src/analysis/minimax_motion_coach.py`:
  - helpers de presence/attente robustes (`_wait_for_page_condition`, `_motion_coach_cta_present`, `_motion_coach_card_present`, `_experts_search_box_present`),
  - retry de la page expert directe avant fallback,
  - fallback experts sans dependance obligatoire au champ de recherche,
  - erreurs finales plus semantiques (`entry unavailable`, `chat CTA unavailable`, `composer unavailable`),
  - warnings explicites pour distinguer retry direct vs fallback experts.
- Tests ajoutes/ajustes dans `src/tests/test_minimax_motion_coach.py`:
  - retry de la page expert directe avant fallback,
  - ouverture via carte expert meme si `Search experts` est absent,
  - ancien test CTA adapte au nouveau bootstrap.
- Validation locale:
  - `python3 -m py_compile analysis/minimax_motion_coach.py tests/test_minimax_motion_coach.py` OK
  - `python3 -m unittest discover -s tests -v` => 66/66 OK
- Verification navigateur reelle (session MiniMax connectee): sur `https://agent.minimax.io/expert/chat/362683345551702`, clic CTA `Type to chat with AI Motion Coach` puis presence validee de `.tiptap-editor`, `input[type=file]`, `#input-send-icon`.
- Push final: `a0a040d` `fix minimax browser bootstrap fallback`.
- Render: auto-deploy `dep-d6lahu3h46gs73eifgo0` passe live a 14:17 (March 6, 2026).
- Nouveau test user apres build `a0a040d`: logs Render `02:22:01 PM` montrent un nouvel echec `MiniMax browser flow failed: AI Motion Coach entry unavailable` sur instance `[rx67v]`.
- Cause probable: pendant l'ouverture de l'expert, MiniMax peut encore afficher un etat non authentifie ou incomplet; le code concluait a `entry unavailable` sans tenter une re-auth ni journaliser l'etat exact de la page.
- Correctif local applique:
  - `_motion_coach_page_state` ajoute pour logger `url/title/composer/cta/card/search/login_modal`,
  - `_open_motion_coach_chat` accepte maintenant `email/password` et force une re-auth unique si login modal ou etat incomplet pendant l'ouverture,
  - les appels internes (`_run_minimax_browser_only_once`, `_upload_and_send_via_browser`) propagent les credentials browser a `_open_motion_coach_chat`.
- Tests ajoutes:
  - re-auth si la page directe affiche encore le login modal.
- Validation locale:
  - `python3 -m py_compile analysis/minimax_motion_coach.py tests/test_minimax_motion_coach.py` OK
  - `python3 -m unittest discover -s tests -v` => 67/67 OK
- Nouveau retour user `erreur` apres build live `85cccae`; audit Render live relu.
- Cause exacte identifiee: le navigateur headless Render tombait sur un interstitiel anti-bot MiniMax/Cloudflare (`title: Just a moment...`) avant meme l'ouverture AI Motion Coach; l'erreur precedente `AI Motion Coach entry unavailable` n'etait qu'un symptome DOM secondaire.
- Correctif local applique dans `src/analysis/minimax_motion_coach.py`:
  - profil navigateur rendu plus proche d'un navigateur reel (viewport, locale, timezone, user-agent, headers),
  - injection stealth (`navigator.webdriver`, `languages`, `plugins`, `platform`, `window.chrome`, permissions),
  - injection best-effort des cookies MiniMax si `MINIMAX_COOKIE` existe,
  - detection explicite de challenge bot via titre/body (`Just a moment`, `checking your browser`, etc.),
  - attente active + reload unique avant echec semantique `blocked by anti-bot challenge`,
  - protection appliquee autant au refresh session browser qu'a l'ouverture AI Motion Coach.
- Tests ajoutes dans `src/tests/test_minimax_motion_coach.py` pour couvrir la resolution ou la persistance du challenge bot.
- Validation locale finale:
  - `python3 -m py_compile analysis/minimax_motion_coach.py tests/test_minimax_motion_coach.py` OK
  - `python3 -m unittest tests.test_minimax_motion_coach -v` OK (30/30)
  - `python3 -m unittest discover -s tests -v` OK (69/69)
- Commit/push final: `e5cc3e9` `fix minimax anti-bot browser bootstrap`.
- Render deploy `dep-d6lb2jkhg0os73bsdn90` observe live via dashboard a `02:52:47 PM` avec logs startup OK et `Your service is live`.

## Formcheck MiniMax strict
- Exigence user revalidée: tout doit passer par MiniMax AI Motion Coach, aucun fallback local pour l'analyse.
- Audit du flux browser-only: le vrai bug restant venait surtout de deux points.
  - Prompt trop lourd/contre-productif qui poussait MiniMax vers des sorties verbeuses et coûteuses.
  - Parseur local incapable de lire la sortie réelle finale de MiniMax quand elle était encapsulée dans le `Thinking Process`.
- Correctifs livrés:
  - nouveau prompt beaucoup plus court avec labels stables (`EXERCISE`, `SCORE`, `REPS_TOTAL`, etc.)
  - rejet explicite des sorties intermédiaires MiniMax (`Thinking Process`, `Completed Skill`, `Ongoing Command Line Execution`)
  - parseur robuste des labels MiniMax, même quand ils sont inline avec underscores décoratifs
  - métadonnées `motion_coach_validated` fiabilisées via le browser expert flow
- Validation réelle effectuée sur une vidéo d'exercice:
  - MiniMax browser-only a répondu avec une analyse complète parsée correctement
  - résultat obtenu: `smith_machine_bench_press`, `75/100`, `9 reps`, intensité `70/100`, repos moyen `1.5s`
- Validation code:
  - `python3 -m unittest tests.test_minimax_motion_coach -v` OK
  - `python3 -m unittest discover -s tests -v` OK

- Blocage `analyse en cours`: root cause confirmée = aucun process local `app.minimax_remote_worker` actif alors que la prod Render est en `remote_worker_enabled=true` et `browser_only=true`.
- Dashboard Render audité en live: `MINIMAX_REMOTE_WORKER_TOKEN`, `MINIMAX_BROWSER_EMAIL`, `MINIMAX_BROWSER_PASSWORD` bien présents; lancement d’un worker local persistant en préparation avec profil navigateur MiniMax déjà validé.
