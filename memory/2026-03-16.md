# 2026-03-16

- Diagnostique prod Formcheck MiniMax: le blocage `toujours en attente` n etait pas dans Render ni dans le browser MiniMax, mais dans la queue elle-meme.
- Preuve recue en prod via shell web + logs Render:
  - dernier job `minimax_remote_jobs.id=6` claim par `MacBook-Pro-de-achkan.local-60518`
  - erreur finale `MiniMax global analysis timeout reached`
  - logs web montrant des `POST /internal/minimax/jobs/claim` recurrents depuis une IP publique externe
- Correctif code: blocage cote serveur des workers non Render sur l endpoint `/internal/minimax/jobs/claim` avec allowlist par `worker_id`.
- Tests ajoutes pour le helper de validation et pour l endpoint de claim.
- Second diagnostic prod le meme jour:
  - worker Render redeploye et `worker_id` derive correctement en `srv-*`
  - mais claim manuel depuis le shell worker => `403 {"detail":"Invalid internal token"}`
  - cause racine: derive d auth entre `MINIMAX_REMOTE_WORKER_TOKEN` et `RENDER_API_KEY`
- Correctif code: le web accepte maintenant tous les tokens internes valides configures pour les endpoints worker MiniMax, au lieu d un seul token prioritaire.
- Validation locale apres correctif auth: `162 passed, 2 skipped`.
- Troisieme diagnostic prod le meme jour:
  - claim manuel depuis le shell worker passe ensuite en `200`
  - mais la queue ne bouge toujours pas automatiquement
  - inspection shell: service `live`, mais seulement wrapper `/bin/sh /usr/bin/xvfb-run ...` + `Xvfb`, sans vrai process Python worker durable visible
- Correctif code suivant: abandon du reexec `xvfb-run` au profit d un demarrage direct d `Xvfb` depuis `app.minimax_remote_worker`, pour coupler la vie du display et du vrai process Python worker.
- Validation prod apres `73b4e5e`:
  - la queue est enfin consommee automatiquement par le worker Render
  - le job `id=6` n est plus bloque en `queued`
  - nouveau defaut trouve: job passe en `failed` avec `404 /internal/minimax/jobs/6/video`
- Diagnostic final de ce run:
  - `video_path='media/videos/e168cb58-1b27-4ffe-8d01-2662d3fcffdf.mp4'`
  - fichier absent cote web au moment du fetch worker
  - cause racine: stockage media job hors disque persistant (`/app/state`)
- Correctif code suivant: stockage media bascule vers `/app/state/media` quand disponible, avec fallback local pour le dev.
- Nouveau test reel apres fix media:
  - queue consommee correctement
  - job `id=7` cree avec `video_path='/app/state/media/videos/44a20a9b-8ca4-4594-972b-c2a884a67dbe.mp4'`
  - nouveau defaut trouve: echec Playwright Chromium avec profil deja utilise
- Diagnostic final de ce run:
  - erreur worker: `The profile appears to be in use by another Chromium process`
  - cause racine: lancement direct sur le profil persistant partage MiniMax au lieu d un workspace temporaire par run
- Correctif code suivant: cloner le seed profil MiniMax dans un workspace temporaire par run, supprimer les locks `Singleton*`, lancer Chromium dessus puis nettoyer ce workspace a la fin.
- Verification suivante apres ce fix:
  - nouvelle video inbound a `2026-03-16 10:07:18`
  - `analysis_id=17`, `job_id=8`
  - la queue a bien fonctionne: job claim par `srv-d6o382rh46gs73a59h8g-87dfd5ddd-trxr4-1`
  - message outbound envoye au client a `2026-03-16 10:13:18`: `Analyse MiniMax temporairement indisponible...`
  - cause active du run: `MiniMax global analysis timeout reached`
  - conclusion: la queue et le worker sont sains; le point de panne restant est le temps d analyse MiniMax sur certaines videos
- Verification live du test suivant:
  - webhook WhatsApp a `2026-03-16 10:53:03`
  - media telecharge et sauvegarde `9002308 bytes`
  - `analysis_id=18`, `job_id=9`
  - video job servie au worker en `200` a `2026-03-16 10:53:05`
  - logs worker a `2026-03-16 10:53:24`: overlay MiniMax `MaxClaw Team Mode is here`
  - correction code locale: suppression DOM elargie a toute promo overlay `MaxClaw` et purge active de cet overlay pendant la boucle d attente apres l envoi
  - validation locale: `pytest -q tests/test_minimax_motion_coach.py` -> `92 passed`
- Reprise d incident suite au retour utilisateur `plus rien ne marche`:
  - audit local du code MiniMax apres les derniers deploys
  - constat: la suite MiniMax pouvait encore attendre passivement jusqu au timeout global si le chat MiniMax cesse de se rafraichir apres l envoi video, surtout sur les clips longs/lourds
  - correctifs appliques dans `analysis/minimax_motion_coach.py`:
    - attente d attachement video dimensionnee par taille de fichier
    - attente du bouton d envoi portee a 15s
    - refresh actif du chat `sent_chat_id` quand l UI ne donne plus de signal
    - metadata `wait_refreshes` pour confirmer les recoveries de polling
    - timeout effectif passe de `180 + duree*5` a `180 + duree*6 + tailleMB*3`, cap a `900s`
  - config: `minimax_max_effective_timeout_s` par defaut passe de `300` a `900`
  - regressions ajoutees:
    - refresh actif du chat quand l UI stagne
    - timeout etendu pour video longue/lourde
  - validation locale:
    - `pytest -q tests/test_minimax_motion_coach.py` -> `94 passed`
    - `pytest -q tests/test_remote_minimax_worker_flow.py tests/test_runtime_config.py` -> `26 passed`
    - `pytest -q` -> `169 passed, 2 skipped`

## Session lundi 16 mars 2026 — Bilans + Ads + Projets

### Bilans clients traités (drafts créés)
1. Mika Giovanni — question créatine Eric Favre confirmée OK, protocole 3g/jour
2. Lesley Rmk — bilan S2 complet, poids moyen 84,64 kg, tendance negative, cigarette bien engagée
3. Vincent Marchebout — bilan S7 depuis Guadeloupe, douleur épaule gauche depuis S6 à surveiller
4. Valentin Meddour — bilan S1 Withings, poids 78,3 → 76,8 kg, HIIT mal placé (après muscu), mobilité 30%
5. Maxime (gims.pay) — questions programme (riz cru, légumes volonté, repos, marche blocs, foot)
6. Selma Belkhiri — bilan S11, poids stable 59,9-60,1 kg, progression charges, barbecue géré
7. Théo Ribois — bilan S1, 3/4 séances, diet 5/10, premier squat barre libre
8. Shehryar ISO — bilan S7, 118,2 kg, déplacement Italie/Suisse, lui demander 7 poids à jeun dès S8
9. Arnaud Durst — bilan S3 judo, poids 80,3 kg, douleur psoas depuis mercredi, premières érections matinales (signal hormonal positif)
10. Florent Désiré — a reçu 2x programme entraînement mais pas sa diète (erreur pièce jointe)
11. Zaid Belkadi — bilan S3, poids 80,93 kg stable, transit constipé (crottes de lapin)
12. Lucas Misslin — bilan S2, moyenne 121,46 kg, repas 1 oublié, journée 2616 pas
13. Julien Sammarcelli — fatigue nerveuse centrale, séance bras + 19000 pas la veille
14. Christopher Dieudonné — crash nerveux total, 0 envie lundi mardi, épaule bilatérale, capoeira raide
15. Gill Kali — a renouvelé le coaching

### Erreur process à retenir
Achzod a signalé que les bilans du lundi étaient trop courts, trop basiques, sans design ni template premium.
Règle fixée : jamais compresser un bilan du lundi. Soit template premium complet, soit on ne draft pas encore.
Standard obligatoire : HTML inline, tableau KPI, analyse dense, ton humain, sections structurées.

### Projet Ads Google — APEX Discovery Scan
6 vidéos reçues dans /Users/achzod/Desktop/ads google/
5 transcrites et analysées, 1 encore en cours (creative_06.mp4 grosse taille 17M)

Classement final :
1. Creative 02 = MEILLEURE (cas Kevin, chiffres, avant/après, CTA audit gratuit) → front end APEX
2. Creative 01 = BONNE (flexibilité métabolique, CTA audit gratuit) → variante A/B APEX
3. Creative 03 = Anabolic Code front end
4. Creative 04 = Anabolic Code retargeting
5. Creative 05 = Reel Insta uniquement (oméga 3 trop technique)
6. Creative 06 = non encore analysée

Décision stratégique validée :
Meilleure offre d'acquisition pour les ads = APEX Discovery Scan GRATUIT, pas le coaching direct
Logique funnel : ads vidéo → Discovery Scan → relance → upsell

Fichiers Stories aussi présents : /Users/achzod/Downloads/achzod-stories-insta/ (Story1 à Story8 + StoryAP StoryQuiz StorySP StoryYT) — à analyser

### Réflexions business évoquées
1. Système de relance et conversion prospects coaching (lead scoring + sequences multi-canal + CRM)
2. Micro SaaS coaching fitness
3. Free tool + capture email + upsell
4. Trading algo : déconseillé, edge requis
5. Google Ads : bonne piste si funnel solide derrière

### Notes techniques
Whisper CLI disponible, modèle turbo par défaut
Pour les grosses vidéos (17M+), Whisper turbo très lent → utiliser tiny ou small pour analyse rapide
Dossier travail vidéos : /Users/achzod/clawd/inbox_ads/2026-03-16/full6/

## Session après-midi — Browser & Google Ads

### Tentative contrôle Chrome pour Google Ads
1. Achzod veut que je crée ses campagnes Google Ads moi-même, pas en mode guidé.
2. Le browser tool OpenClaw ne peut pas se connecter au Chrome utilisateur — DevToolsActivePort introuvable.
3. Tentatives multiples : remote-debugging-port=9222, kill+relaunch, profil Default — aucune n'a fonctionné.
4. L'extension OpenClaw Browser Relay n'existe PAS chez Achzod. Ne plus jamais la mentionner.
5. Playwright installé dans le workspace. Script run_ads_setup.js créé pour ouvrir Chrome avec le vrai profil Google et naviguer sur ads.google.com.
6. Le script copie le profil Chrome Default dans un dossier temporaire pour contourner le lock ProcessSingleton.

### Google Ads — Accès
1. Customer ID : 877-237-2333
2. Invitation admin envoyée à achkou@gmail.com et acceptée.
3. L'API Google Ads ne permet PAS de créer des campagnes vidéo YouTube. Seules Performance Max et Demand Gen sont supportées via API.
4. La seule option pour créer une campagne YouTube in-stream est soit via l'interface web, soit via un script Playwright qui navigue dans l'interface.

### Skills installés cette session
1. claw-google-ads (skill ClawHub) — monitoring/gestion campagnes existantes via API Python, pas de création YouTube
2. playwright (skill ClawHub) — browser automation via Playwright

### Leçon critique
Achzod déteste quand je lui fais faire des manipulations techniques Terminal. Il veut que JE fasse le travail, pas qu'il suive des instructions step by step. "c pas compliqué d'ouvrir chrome et naviguer" = frustration légitime. Ne plus jamais proposer de mode guidé sauf si absolument aucune autre option.

### Modèle
Achzod a demandé opus 4.6 explicitement. Pas 4.5. Toujours respecter la version exacte demandée.

- 15:21 Reprise incident prod critique apres retour utilisateur 'plus rien ne marche'. Fix overlay 2637bdf deja live sur web+worker; prochaine etape: tracer le dernier run casse apres deploy pour identifier la panne active restante.
